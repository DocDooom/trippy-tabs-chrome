// Service Worker runs when needed deactivates when it's not
// Will lose the state when deactivated
// To get persistent state we need to store it on users HDD
chrome.runtime.onInstalled.addListener(() => {
    chrome.
});
